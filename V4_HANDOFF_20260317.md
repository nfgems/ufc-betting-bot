# UFC V4 Handoff - 2026-03-17

## Latest Local Update - 129 Feature V4 State

This section supersedes the earlier "first-pass v4" status below and captures
the latest local worktree state after the follow-up add-backs completed in this
session.

### Current contract state

- `full_live_contract_v4` is now `129` features
- current `v4` = strict `v3` baseline (`113`) plus:
  - height / reach / age (`9`)
  - `is_empty_arena` (`1`)
  - weight (`3`)
  - stance-derived features (`3`)

So the current gap from original `v2` (`144`) is now `15` features, not `22`.

Remaining missing families are exactly:

- moneyline implied probabilities (`3`)
- rankings (`6`)
- method odds (`6`)

### Current local code state

Latest local worktree changes relevant to `v4` now include:

- strict live lookup for `pulled_all` specs no longer falls back to the loose
  profile-averaged path just because some profile fields were re-enabled
- `is_empty_arena` is now wired into both training and live via the requested
  Apex-first heuristic
- unknown stance now stays `NaN` instead of `-1`
- `v4` now re-enables:
  - `a_weight`, `b_weight`, `diff_weight`
  - `a_stance_enc`, `b_stance_enc`, `same_stance`

Still explicitly not done:

- no retrain after the move from `123` to `129`
- no refreshed offline comparison against `v2`
- no refreshed provenance audit after the move from `123` to `129`
- no promotion
- no live switch
- no commit
- no push
- no restart

### Focused validation completed on the 129-feature code path

Passed:

- `python -m pytest tests/test_v4_profile_enrichment.py -q`
- `python -m pytest tests/test_phase2_schema_contract.py -q -k "strict_live_scrape_recovers_sparse_history_fields or strict_live_sparse_history_preserves_honest_td_nan_rates or live_same_stance_preserves_unknown_as_nan"`

Key factual checks completed:

- `full_live_contract_v3`: `113` features
- `full_live_contract_v4`: `129` features
- requested live profile fields for `v4`:
  - `age`
  - `height`
  - `reach`
  - `stance`
  - `weight`

### Current null audit for the 129-feature v4 contract

Command run:

- `python scripts/audit_model_feature_nulls.py --spec full_live_contract_v4 --json --allow-dead-features`

Result:

- rows total: `8571`
- trainable rows: `4794`
- train split rows: `3374`
- test split rows: `1420`
- materialized contract columns missing: `0`
- dead contract columns on trainable rows: `0`
- dead contract columns on train split: `0`
- suspicious default fills on trainable rows: `0`

Additional exact null count computed on the actual train split:

- total contract cells: `3374 * 129 = 435246`
- total `NaN` cells: `1464`
- overall train-split null rate: `0.3364%`

Important interpretation:

- the current `129`-feature contract is audit-clean
- the `1464` nulls are not evidence of fabricated data
- most of them are honest undefined values and should **not** be blanket-filled

Breakdown of the `1464` train-split `NaN`s:

- takedown accuracy / defense family: `1091`
  - `diff_roll_td_acc = 313`
  - `diff_roll_td_def = 222`
  - `b_roll_td_acc = 196`
  - `b_roll_td_def = 134`
  - `a_roll_td_acc = 132`
  - `a_roll_td_def = 94`
- reach family: `305`
  - `diff_reach = 128`
  - `b_reach = 110`
  - `a_reach = 67`
- stance family: `30`
  - `same_stance = 15`
  - `a_stance_enc = 9`
  - `b_stance_enc = 6`
- age family: `23`
  - `b_age = 11`
  - `diff_age = 11`
  - `a_age = 1`
- everything else combined: `15`

Judgment:

- do **not** target "remove all 1464 NaNs" as a goal by itself
- the `1091` takedown-rate nulls are mostly the kind of honest undefined values
  that should stay `NaN`
- the recoverable null work should focus on source gaps, not destroying honest
  sparsity

### What looks honestly recoverable next

#### 1. Secondary-source stance recovery

Raw fighter-profile coverage currently is:

- weight present on `4455 / 4455`
- stance present on `3606 / 4455`
- stance blank on `849 / 4455`

Important repo fact:

- the current fallback layer does **not** solve missing stance
- `src/data/fallback_scrapers.py` explicitly leaves Sherdog stance blank
- the Tapology fallback path is still a stub because of Cloudflare

So if we want to shrink stance nulls honestly, the next real step is to build a
canonical secondary stance enrichment artifact for the `849` UFCStats blanks,
with explicit source tagging and reproducible merging rules.

#### 2. Reach / profile gap audit

The remaining reach/age/height/weight nulls are small enough to inspect row by
row. Early samples suggest many of the reach gaps come from older UFC fighters
whose UFCStats profile reach value is effectively missing (`--`), not from a
join failure.

This is worth confirming before adding another source, because:

- reach is the largest remaining recoverable null family after TD-rate nulls
- a second-source reach backfill could reduce `305` contract nulls if the
  source is trustworthy and reproducible

#### 3. Tiny cleanup nulls

There are only a handful of residual non-family nulls worth auditing directly:

- `num_rounds_feat = 3`
- `diff_height = 1`
- `b_height = 1`
- `diff_weight = 1`
- `b_weight = 1`
- `diff_roll_str_def = 4`
- `b_roll_str_def = 3`
- `a_roll_str_def = 1`

These should be resolved by direct row inspection before any broad refactor.

### Path from 129 to 144

The missing `15` features are **not** null-repair work. They are entirely the
still-excluded historical families:

1. moneyline implied probabilities (`3`)
2. rankings (`6`)
3. method odds (`6`)

That means there are really two separate tracks:

1. shrink recoverable nulls inside the current `129`
2. recover the blocked `15` feature family members to reach `144`

Recommended order:

1. keep the honest `129` contract stable
2. recover secondary-source stance if feasible
3. inspect whether any reach gaps can be filled honestly
4. rerun null + provenance audit
5. retrain and compare against `v2`
6. then work the historical-archive problem for moneyline / rankings / method
   odds

### How to think about the Master Audit report versus feature work

The right answer is **not** "do all audit later" and it is also **not** "stop
all feature work immediately".

Recommended split:

#### Do these before or alongside more feature-contract work

These findings touch model/data correctness directly and should be folded into
the same branch of work:

- `src/data/fighter_lookup.py` historical look-ahead via `datetime.now()`
- `src/features/build_features.py` duplicate/shadowed helpers/constants
- silent coercion paths that can fabricate neutral-looking values

Reason:

- these are not deployment-hardening issues
- they directly affect trust in model features and offline evaluation
- delaying them while adding more feature families risks stacking new work on
  top of code we already know is easy to misread or subtly wrong

#### Track these separately, but treat them as hard blockers before live use

These are urgent production-risk items, but they are not blockers for offline
feature-recovery experiments if this branch is not placing live money:

- order execution durability / idempotency
- unauthenticated web mutation endpoints
- bankroll sync / live blend-path issues
- deployment hardening

My recommendation:

- keep pushing feature/data recovery now
- but fix the model-data-adjacent audit items **before** trying to close the
  final gap to `144`
- do not ship any new live trading path until the critical execution/web audit
  items are addressed

### If I were continuing from here

Immediate next steps I would take:

1. fix the data-adjacent audit items first:
   - historical `datetime.now()` leakage in `fighter_lookup`
   - shadowed helpers/constants in `build_features`
2. implement a real secondary stance enrichment path for the `849` UFCStats
   blanks
3. inspect the `305` reach-family nulls and determine how many are source-side
   recoverable
4. rerun:
   - feature null audit
   - provenance audit
   - candidate retrain
5. only then move to the remaining `15` blocked features on the road to `144`

## Current Repo State

- Repo: `C:\Users\Evan\betting-bot\ufc-betting-bot`
- Branch: `recovery/repo-cleanup-20260317`
- Latest local checkpoint mentioned by user: `9c9167428b4699efd4c425bfd7cf4943334b5887`
- Git remote: `origin https://github.com/nfgems/ufc-betting-bot.git`

### Uncommitted local changes currently present

These are real code changes in the worktree and were intentionally **not** promoted or pushed:

- `src/data/fighter_lookup.py`
- `tests/test_phase2_schema_contract.py`

They fix the live strict sparse-history scrape gap for `Lerryan Douglas`:

- parse UFCStats event dates embedded in profile fight rows
- derive strict `roll_*` rate fields from observed UFCStats fight stats
- carry detail-page weight class for `wc_move`
- preserve honest `NaN` for undefined takedown-rate percentages

## What Was Validated Before Pausing Promotion

### Full repo tests

- `382 passed, 1 skipped`

### Strict `v3` audits

- `python scripts/audit_model_feature_nulls.py --spec full_live_contract_v3 --json`
  - `materialized_contract_columns_missing=[]`
  - `dead_contract_columns_trainable=[]`
  - `dead_contract_columns_train_split=[]`
  - `dead_contract_columns_recent=[]`
  - `suspicious_default_fills_trainable=[]`
  - `suspicious_default_fills_recent=[]`

- `python scripts/audit_feature_provenance.py --spec full_live_contract_v3 --json`
  - no legacy-dependent rows in the `v3` contract

### Exact live odds-backed smoke

Fight:

- `Julian Erosa vs Lerryan Douglas`
- commence time: `2026-03-28 23:00:00+00:00`
- event id: `02af202154f1a343e5da93873e6a81d2`

Result after sparse-history fix:

- strict contract features: `113`
- missing keys: `0`
- remaining `NaN` features: `4`
- remaining `NaN` columns:
  - `b_roll_td_acc`
  - `b_roll_td_def`
  - `diff_roll_td_acc`
  - `diff_roll_td_def`

Interpretation:

- these `4` are honest undefined values
- they come from a lone UFCStats fight where both sides attempted `0` takedowns
- they are **not** a scrape gap

## Why Promotion Was Stopped

Promotion was paused because the user challenged the assumption that `v3` should be the final release candidate.

That challenge is valid.

`v3` is not just a cleaner `v2`. It is a deliberately smaller provenance-strict subset:

- `v2`
  - spec: `full_live_contract_v2`
  - dataset variant: `best_of_both_full_history`
  - feature count: `144`
  - no-odds feature count: `135`

- `v3`
  - spec: `full_live_contract_v3`
  - dataset variant: `pulled_all`
  - feature count: `113`
  - no-odds feature count: `113`

The reduction was intentional. `v3` excludes feature families defined in `src/model/training_spec.py` under `PROVENANCE_STRICT_EXCLUDED_FEATURE_COLS`.

Excluded families:

- physical profile
- stance
- moneyline implied probabilities
- rankings
- method odds
- `is_empty_arena`

The user is likely right that some of these families are recoverable and valuable, but the repo today does **not** prove that all of them are historically trustworthy in the canonical `pulled_all` training path.

## Important Source Findings For V4

### 1. `pulled_all` is intentionally treated as diagnostic

`src/strategy/ufc_source_compare.py` marks:

- `_NON_PROMOTABLE_VARIANTS = {"pulled_all"}`

This is strong evidence that `v3` should be viewed as a floor / audit baseline, not automatically the production endpoint.

### 2. Physical attributes are the best near-term add-back candidate

Evidence:

- `src/data/ufc_refresh.py` already has helper hooks:
  - `_load_scraped_fighter_lookup(...)`
  - `_extract_fighter_attributes(...)`

Those helpers can populate:

- `height`
- `reach`
- `weight`
- `stance`
- `age`

However, the current pulled-data training path does **not** use them:

- `build_training_rows_from_pulled_data(...)`
- calls `_scraped_fights_to_training_rows(scraped_like_df, fighter_lookup={})`

That means the current `pulled_all` path deliberately drops fighter-profile attributes even though helper code exists.

### 3. The canonical repo does not currently contain train-ready historical fighter profile snapshots

Current committed raw file:

- `data/raw/ufc-fighter-details.csv`

It contains only:

- `FIRST`
- `LAST`
- `NICKNAME`
- `URL`

It does **not** contain:

- height
- reach
- weight
- stance
- DOB / age

Expected richer file path from scraper code:

- `data/raw/ufc_fighters_scraped.csv`

That file is **not** present in the repo, and does not appear committed in current history.

Conclusion:

- physical add-back is plausible
- but it still requires generating or recovering a real historical fighter-profile dataset

### 3A. Fresh UFCStats profile scraping is confirmed to work now

This was directly re-checked during the current session against the existing
`data/raw/ufc-fighter-details.csv` URL list.

Sample command path:

- import `scrape_fighter(...)` from `src/data/scraper.py`
- fetch the first 5 fighter-detail URLs already present in the repo

Observed results:

- `Tom Aaron`
  - height: `--`
  - reach: `--`
  - weight: `155 lbs.`
  - stance: blank
  - dob: `Jul 13, 1978`
- `Danny Abbadi`
  - height: `5' 11"`
  - reach: `--`
  - weight: `155 lbs.`
  - stance: `Orthodox`
  - dob: `Jul 03, 1983`
- `Nariman Abbasov`
  - height: `5' 8"`
  - reach: `66"`
  - weight: `155 lbs.`
  - stance: `Orthodox`
  - dob: `Feb 01, 1994`
- `Darion Abbey`
  - height: `6' 2"`
  - reach: `80"`
  - weight: `265 lbs.`
  - stance: `Orthodox`
  - dob: `Feb 25, 1993`
- `David Abbott`
  - height: `6' 0"`
  - reach: `--`
  - weight: `265 lbs.`
  - stance: `Switch`
  - dob: `Apr 26, 1965`

Interpretation:

- a fresh canonical `ufc_fighters_scraped.csv` is realistically generatable from UFCStats today
- height / reach / weight / stance / DOB are not hypothetical fields; the scraper already extracts them
- missingness still exists for some fighters and must remain honest
- age can be derived honestly from DOB + fight date once the fighter profile artifact exists

Important caveat:

- height, reach, and DOB are relatively stable profile fields
- stance and especially profile `weight` should still be reviewed as profile-snapshot fields, because they may not represent a perfect fight-time historical snapshot if a fighter later changes divisions or presentation
- this does **not** block trying a `v4` candidate, but it should be called out explicitly in the audit notes

### 4. Rankings are live-snapshot capable but not historically archived in the canonical repo

Evidence:

- `src/data/rankings_scraper.py` is snapshot-backed
- `data/raw/rankings/` is present as a directory
- but the directory is currently empty

There is also an older merge path:

- `scripts/merge_kaggle_data.py`
- expects `data/raw/kaggle_rankings/rankings_history.csv`

But:

- `data/raw/kaggle_rankings/` is missing
- `.gitignore` explicitly excludes `data/raw/kaggle_rankings/`

Conclusion:

- rankings are not train-ready for a historically honest `v4` today
- unless an old archive or backup is recovered

### 5. Method odds are live-snapshot capable but not historically archived in the canonical repo

Evidence:

- `src/data/method_odds.py` is snapshot-backed
- `data/raw/method_odds/` exists
- but the directory is currently empty

Conclusion:

- method odds are not train-ready for a historically honest `v4` today
- unless an old archive or backup is recovered

### 6. Moneyline implied probabilities are not historically complete for the old training window

Evidence:

- `data/raw/historical_odds/historical_odds.csv`
- min event date: `2022-01-15`
- pre-2022 rows: `0`

Conclusion:

- moneyline implied probabilities are not currently honest for the pre-2022 training split
- re-adding them would either:
  - make `v4` legacy-dependent again
  - or require a real pre-2022 historical odds archive / new backfill source

### 7. `is_empty_arena` is likely reconstructable

This is the one excluded non-market feature that looks easiest to reintroduce honestly if desired.

Reason:

- it is event-context level, not fighter-history level
- it should be derivable from event date / known COVID-era event metadata

But this has **not** been implemented yet in the current session.

## Recommendation: Define V4 In Stages

Do **not** try to add everything back at once.

Use a staged plan:

### V4A - Safe add-backs first

Candidate goal:

- keep the strict `pulled_all` dataset foundation
- add back only families we can support from repo-owned or freshly generated UFCStats profile data

Target add-backs:

- `a_height`, `b_height`, `diff_height`
- `a_reach`, `b_reach`, `diff_reach`
- `a_weight`, `b_weight`, `diff_weight`
- `a_age`, `b_age`, `diff_age`
- possibly `a_stance_enc`, `b_stance_enc`, `same_stance`
- possibly `is_empty_arena`

This would give a richer candidate without immediately reintroducing rankings or market-derived history.

### V4B - Only if historical archives are recovered

Potential add-backs if old trustworthy data exists in backups:

- rankings
- method odds
- moneyline implied probabilities

This should happen **only** after proving the historical source is:

- real
- archived
- date-aware
- reproducible
- compatible with provenance auditing

## Recommended Implementation Sequence

### Step 1: Recover or regenerate fighter profile history inputs

Goal:

- produce a real `data/raw/ufc_fighters_scraped.csv` or equivalent canonical artifact

Needed fields:

- fighter name
- height
- reach
- weight
- stance
- DOB

This is the highest-value next task for `v4`.

### Step 2: Wire fighter profile data into `pulled_all`

Likely code path:

- `src/data/ufc_refresh.py`

Specifically:

- stop passing `fighter_lookup={}` in `build_training_rows_from_pulled_data(...)`
- load the real fighter profile lookup from a canonical raw source
- verify attributes are carried into pulled training rows

### Step 3: Add a `full_live_contract_v4_spec`

Do not mutate `v3`.

Create a new explicit spec so comparisons remain clean:

- `full_live_contract_v3`: strict baseline
- `full_live_contract_v4`: strict baseline plus recovered physical/context add-backs

### Step 4: Audit and compare

For `v4`, rerun:

- full tests
- null audit
- provenance audit
- live parity smoke
- offline comparison against `v2` and `v3`

### Step 5: Only then choose promotion target

Possible outcomes:

- promote `v4`
- keep `v3` if added-back families do not materially help
- define a later `v4b` if archive recovery unlocks rankings / odds families

## Suggested Immediate Next Task For The Next Session

The best next move is:

1. Generate or recover a canonical `ufc_fighters_scraped.csv`
2. Patch `src/data/ufc_refresh.py` so `pulled_all` actually uses fighter profile attributes
3. Create `full_live_contract_v4_spec` with physical add-backs only
4. Retrain and audit that candidate

This is the fastest path to a better model without compromising provenance.

## Things Explicitly Not Done

The current session did **not**:

- promote `v3`
- switch root live model artifacts
- change the default promoted spec from `v2` to `v3`
- commit anything
- push anything
- restart any service

That pause was intentional after the user raised the `v4` concern.

## Summary Judgment

The user's claim is directionally correct:

- a richer trustworthy post-`v3` model is likely possible

But the repo does **not** currently prove that all removed families are already historically honest.

The right path is:

- keep `v3` as the strict reference
- build `v4` carefully
- add back the recoverable families first
- treat rankings / method odds / old moneyline as archive-recovery work, not assumptions

## Update After V4 Implementation Pass

The first `v4` implementation pass is now complete in the local worktree.

### Code changes completed

- `src/data/scraper.py`
  - added deterministic fighter-profile scraping from the committed
    `data/raw/ufc-fighter-details.csv` inventory
  - `scrape_all_fighters()` now prefers the repo inventory over a full
    live alphabetical crawl
- `src/data/ufc_refresh.py`
  - `build_training_rows_from_pulled_data(...)` now loads
    `data/raw/ufc_fighters_scraped.csv` and passes the real fighter lookup
    into `_scraped_fights_to_training_rows(...)`
- `src/model/training_spec.py`
  - added `full_live_contract_v4_spec()`
  - `v4` is defined as the `v3` pulled-only baseline plus only:
    - `a_height`, `b_height`, `diff_height`
    - `a_reach`, `b_reach`, `diff_reach`
    - `a_age`, `b_age`, `diff_age`
- `tests/test_v4_profile_enrichment.py`
  - added regression coverage for:
    - inventory-backed fighter scraping
    - pulled-data fighter-profile enrichment
    - `full_live_contract_v4` contract membership

Important scope decision:

- `weight`, `stance`, `same_stance`, rankings, moneyline implied probabilities,
  method odds, and `is_empty_arena` remain excluded from `v4`
- stance was intentionally left out of first-pass `v4` because the current
  training/live encoding treats unknown stance as `-1`, which is not a clean
  provenance-strict add-back yet

### Canonical fighter-profile artifact generated

The canonical raw artifact now exists locally:

- `data/raw/ufc_fighters_scraped.csv`

Generation details:

- source inventory: `data/raw/ufc-fighter-details.csv`
- rows scraped: `4455`
- scrape mode: inventory-backed UFCStats profile scrape with checkpoint saves

Observed profile coverage in the generated artifact:

- height present: `4137`
- reach present: `2515`
- weight present: `4369`
- stance present: `3606`
- DOB present: `3951`

Training log note:

- `src.data.ufc_refresh` loaded `4448` normalized fighter profiles from the
  artifact during training after name-key normalization / duplicate collapse

### V4 audits and training completed

`full_live_contract_v4` now has candidate artifacts under:

- `data/processed/candidates/full_live_contract_v4/`
- `models/candidates/full_live_contract_v4/`

Completed validations:

- full test suite: `385 passed, 1 skipped`
- null audit:
  - `python scripts/audit_model_feature_nulls.py --spec full_live_contract_v4 --json`
  - result: no missing materialized contract columns, no dead train-time
    columns, no suspicious default fills
- provenance audit:
  - `python scripts/audit_feature_provenance.py --spec full_live_contract_v4 --json`
  - result: no legacy-dependent rows in the `v4` contract
  - physical-profile family is repo-owned / pulled-scraped, not legacy-carried
- candidate training:
  - output subdir: `candidates/full_live_contract_v4`
  - train rows: `3374`
  - test rows: `1420`
  - feature count: `122`

Saved audit artifacts:

- `models/candidates/full_live_contract_v4/feature_null_audit.json`
- `models/candidates/full_live_contract_v4/feature_provenance_audit.json`

### Offline comparison result

Same-fight offline comparison was run on the shared `1420` post-2022 fights
available to `v2`, `v3`, and `v4`.

XGBoost results on the shared fight set:

- `v2`
  - accuracy: `0.651408`
  - log loss: `0.632843`
  - brier: `0.221224`
- `v4`
  - accuracy: `0.618310`
  - log loss: `0.653401`
  - brier: `0.230719`
- `v3`
  - accuracy: `0.592254`
  - log loss: `0.663379`
  - brier: `0.235515`

Interpretation:

- `v4` does improve materially over the strict `v3` baseline
- `v4` does **not** beat current `v2` on the shared offline test set
- so the first-pass physical-profile add-back helped, but it was not enough to
  justify promotion over `v2` on offline evidence alone

### Live smoke result

Direct `build_fight_features(...)` live-path smokes were run with
`training_spec=full_live_contract_v4`.

Results:

- `Movsar Evloev vs Lerone Murphy`
  - strict `v4` contract built successfully
  - `122` features present
  - `0` missing keys
  - `0` `NaN` contract features
- `Julian Erosa vs Lerryan Douglas`
  - live-path build succeeded when run in true live mode
    (no historical `as_of_date` fail-closed override)
  - `122` features present
  - `0` missing keys
  - `0` `NaN` contract features
  - recovered profile fields populated:
    - `b_height = 175.26`
    - `b_reach = 182.88`
    - `b_age = 30.9`

Important nuance:

- passing `as_of_date` still forces the historical fail-closed lookup path for
  sparse fighters like `Lerryan Douglas`
- that is distinct from the real upcoming-live inference path, which is why the
  correct parity smoke for `v4` should be interpreted from the live-path run,
  not the historical fail-closed run

### Current judgment after the implementation pass

The first `v4` candidate is real, audited, trainable, and live-build clean.

But the current evidence says:

- `v4` is better than `v3`
- `v4` is still worse than `v2` offline
- therefore `v4` should **not** be promoted yet

The remaining question is no longer whether a trustworthy `v4` can be built.
It can.

The question is whether a later `v4b` can recover additional honest signal
without violating provenance.

Most likely next directions:

1. Evaluate whether `stance` can be reintroduced honestly without the current
   unknown-encoding ambiguity.
2. Decide whether profile `weight` should stay excluded or be admitted with
   explicit fight-time-context caveats.
3. Investigate whether `is_empty_arena` can be reconstructed deterministically
   from event metadata.
4. Leave rankings, method odds, and moneyline implied probabilities out unless
   a real historical archive is recovered and audited.

### Still explicitly not done

- no promotion from `v2` to `v4`
- no promotion from `v3` to `v4`
- no live artifact switch
- no default model reference change
- no commit
- no push
- no restart
