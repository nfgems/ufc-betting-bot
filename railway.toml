[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ALWAYS"
restartPolicyMaxRetries = 5
# Railway sets $PORT automatically. Gate activation on full runtime readiness,
# including model artifacts and the hosted background loops.
healthcheckPath = "/readyz"
healthcheckTimeout = 600
