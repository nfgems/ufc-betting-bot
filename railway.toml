[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ALWAYS"
restartPolicyMaxRetries = 5

# Railway sets $PORT automatically — web dashboard binds to it
[deploy.healthcheck]
path = "/healthz"
interval = 30
timeout = 10
initialDelay = 30
